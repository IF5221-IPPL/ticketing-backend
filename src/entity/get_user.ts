export interface IGetUserResponse {
    userId: string;
    name: string;
	email: string;
    isActive: boolean;
    role: string;
    profilePictureUrl: string;
    createdAt: string;
    updatedAt: string;
}
